services:
  nextjs:
    container_name: nextjs
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.prod
    networks:
      - nextjs
    ports:
      - 3000:3000
networks:
  nextjs:
    driver: bridge